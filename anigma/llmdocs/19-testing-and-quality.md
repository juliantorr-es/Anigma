# Testing and Quality

## Test Execution
- Full test runs are driven by `Scripts/test.sh` as documented in `README.md`.
- Repository tests live under `Tests/` and include daemon and sidecar tests.

## Targeted Test Scripts
- Daemon-specific test harnesses include `test_daemon.sh` at the repo root.

## Linting and Reports
- SwiftLint documentation and reports live alongside root artifacts such as `SWIFTLINT_COMPLETE_GUIDE.md` and `swiftlint_master_execution_report.md`.

## Key References
- `Scripts/test.sh`
- `Tests/`
- `SWIFTLINT_COMPLETE_GUIDE.md`
