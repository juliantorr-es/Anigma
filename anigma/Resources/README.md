Shared static assets and bundled resources.
Invariants: keep assets deterministic and referenced via package resources.

Entry points:
- Resource files under `Resources/`.

Public surface:
- Shared resources for app and services.

Build/test:
- Packaged via SwiftPM resource declarations.

Related docs:
- `../llmdocs/18-build-and-release.md`
