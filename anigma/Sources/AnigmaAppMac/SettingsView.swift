//
//  SettingsView.swift
//  AnigmaAppMac
//

import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Settings")
                .font(.title)
            Spacer()
        }
        .padding()
    }
}

struct PrivacyConsoleView: View {
    var body: some View {
        VStack {
            Text("Privacy Settings")
                .font(.title)
            Spacer()
        }
        .padding()
    }
}
