ColorKit package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/ColorKit/Sources`

Public surface:
- SwiftPM target `ColorKit` (see `Package.swift`).

Build/test:
- `swift build --target ColorKit`

Related docs:
- `../../llmdocs/06-mac-app.md`
- `../../llmdocs/18-build-and-release.md`
