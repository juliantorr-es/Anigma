AnigmaTUI package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/AnigmaTUI/Sources`

Public surface:
- SwiftPM target `AnigmaTUI` (see `Package.swift`).

Build/test:
- `swift build --target AnigmaTUI`

Related docs:
- `../../llmdocs/05-cli.md`
- `../../llmdocs/18-build-and-release.md`
