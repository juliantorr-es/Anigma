// PDFSidecarNativeShims.cpp
// PDF-specific native shim layer
// Isolates PDF/PDFium native linker settings from generic AnigmaNativeShims

#include "PDFSidecarNativeShims.h"

// Empty shim for now - PDF-specific functionality is in PDFNative
// This target serves as an isolation layer for PDF/PDFium linker settings
extern "C" {
    // Placeholder for PDF-specific shim functions if needed
}
