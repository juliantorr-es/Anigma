CompressionKit package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/CompressionKit/Sources`

Public surface:
- SwiftPM target `CompressionKit` (see `Package.swift`).

Build/test:
- `swift build --target CompressionKit`

Related docs:
- `../../llmdocs/08-capsules.md`
- `../../llmdocs/18-build-and-release.md`
