AnigmaHostMac package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/AnigmaHostMac/Sources`

Public surface:
- SwiftPM target `AnigmaHostMac` (see `Package.swift`).

Build/test:
- `swift build --target AnigmaHostMac`

Related docs:
- `../../llmdocs/03-module-index.md`
- `../../llmdocs/18-build-and-release.md`
