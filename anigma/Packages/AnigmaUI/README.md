AnigmaUI package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/AnigmaUI/Sources`

Public surface:
- SwiftPM target `AnigmaUI` (see `Package.swift`).

Build/test:
- `swift build --target AnigmaUI`

Related docs:
- `../../llmdocs/06-mac-app.md`
- `../../llmdocs/07-render-pipeline.md`
