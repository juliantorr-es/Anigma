AnigmaWork package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/AnigmaWork/Sources`

Public surface:
- SwiftPM target `AnigmaWork` (see `Package.swift`).

Build/test:
- `swift build --target AnigmaWork`

Related docs:
- `../../llmdocs/03-module-index.md`
- `../../llmdocs/11-jobs-and-workflows.md`
