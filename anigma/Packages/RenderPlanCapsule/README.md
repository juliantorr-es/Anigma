RenderPlanCapsule package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/RenderPlanCapsule/Sources`

Public surface:
- SwiftPM target `RenderPlanCapsule` (see `Package.swift`).

Build/test:
- `swift build --target RenderPlanCapsule`

Related docs:
- `../../llmdocs/07-render-pipeline.md`
- `../../llmdocs/18-build-and-release.md`
