# Diaplasion happy-path fixture

This directory contains the minimal fixtures consumed by the Diaplasion HAPPY_PATH pipeline runner.

Keep this directory intentionally tiny (1×1 PNG + a small JSON spec) so the deterministic pipeline stays fast and easy to hash.
