AnimationKit package targets and local APIs.
Invariants: respect governance boundaries, keep outputs deterministic, avoid direct IO outside authorities.

Entry points:
- `Packages/AnimationKit/Sources`

Public surface:
- SwiftPM target `AnimationKit` (see `Package.swift`).

Build/test:
- `swift build --target AnimationKit`

Related docs:
- `../../llmdocs/07-render-pipeline.md`
- `../../llmdocs/18-build-and-release.md`
