{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://anigma.dev/schemas/td-task.schema.json",
  "title": "TD Task Descriptor Schema",
  "description": "Schema for validating Anigma task descriptors in Docs/td/. Each task must have a corresponding task.yaml file that conforms to this schema.",
  "type": "object",
  "required": [
    "id",
    "title",
    "priority",
    "status",
    "type"
  ],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Unique task identifier. Must match the directory name or be a short UUID-like identifier.",
      "examples": ["td-30231dd", "td-a84da2", "fd-009"]
    },
    "title": {
      "type": "string",
      "minLength": 10,
      "description": "Human-readable task title. Minimum 10 characters."
    },
    "priority": {
      "type": "string",
      "enum": ["P0", "P1", "P2", "P3", "P4"],
      "description": "Task priority level."
    },
    "type": {
      "type": "string",
      "enum": ["task", "subtask", "research", "verification", "publication", "maintenance"],
      "description": "Type of task.",
      "default": "task"
    },
    "status": {
      "type": "string",
      "enum": ["new", "ready", "in_progress", "in_review", "complete", "done", "blocked", "cancelled", "archived"],
      "description": "Current status. Must match the directory location under Docs/td/."
    },
    "lane": {
      "type": "string",
      "minLength": 1,
      "description": "Functional grouping lane for this task.",
      "examples": ["daemon-runtime-isolation", "documentation-infrastructure", "architecture-governance"]
    },
    "summary": {
      "type": "string",
      "minLength": 10,
      "description": "One-sentence summary for quick scanning."
    },
    "description": {
      "type": "string",
      "description": "Detailed description of the task's purpose and scope."
    },
    "parent_epic": {
      "type": ["string", "null"],
      "minLength": 1,
      "description": "ID of the epic this task belongs to.",
      "examples": ["p0-004", "p1-td-source-of-truth-recovery"]
    },
    "epic": {
      "type": ["string", "null"],
      "minLength": 1,
      "description": "Alias for parent_epic."
    },
    "worktree": {
      "type": ["string", "null"],
      "description": "Default worktree path for this task. Should be a directory path relative to repo root.",
      "examples": ["anigma/", "docs/", "Scripts/"]
    },
    "recommended_worktree": {
      "type": ["string", "null"],
      "description": "Alias for worktree, preferred field name."
    },
    "source_docs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of paths to source documentation that define this task."
    },
    "related_docs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of paths to related documentation (roadmaps, ADRs, architecture docs)."
    },
    "related_diagrams": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of paths to related diagrams/flowcharts."
    },
    "proofs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of paths to proof artifacts under Docs/proofs/ for this task."
    },
    "proof_required": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of required proof artifacts that must exist before this task is complete."
    },
    "expected_proof": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of expected proof artifacts that will be generated by this task."
    },
    "acceptance": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Task-level acceptance criteria."
    },
    "no_goals": {
      "type": ["string", "null"],
      "description": "Single string describing what this task is NOT about."
    },
    "non_goals": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Task-level non-goals."
    },
    "test_plan": {
      "type": ["string", "null"],
      "description": "Test plan or verification steps for this task."
    },
    "test_commands": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of test commands to run to verify this task."
    },
    "notes": {
      "type": "string",
      "description": "Free-form notes about this task."
    },
    "risk": {
      "type": ["string", "null"],
      "description": "Risk assessment for this task."
    },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of task/epic IDs that must be completed before this task can start."
    },
    "depends_on": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Alias for dependencies."
    },
    "blocks": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of task/epic IDs that this task blocks."
    },
    "blocked_by": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of task/epic IDs that are blocking this task."
    },
    "assigned_to": {
      "type": ["string", "null"],
      "description": "Assigned agent or human for this task."
    },
    "claimant": {
      "type": ["string", "null"],
      "description": "Who claimed this task for execution."
    },
    "created": {
      "type": "string",
      "format": "date",
      "description": "Date when this task was initially created."
    },
    "updated": {
      "type": "string",
      "format": "date",
      "description": "Date when this task was last updated."
    },
    "started": {
      "type": ["string", "null"],
      "format": "date",
      "description": "Date when work started on this task."
    },
    "completed": {
      "type": ["string", "null"],
      "format": "date",
      "description": "Date when this task was completed."
    },
    "completion_date": {
      "type": ["string", "null"],
      "format": "date",
      "description": "Alias for completed."
    },
    "verification_profiles": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "List of verification profile IDs from verification-profiles.yaml that apply to this task."
    },
    "verification_overrides": {
      "type": "object",
      "additionalProperties": true,
      "description": "Override verification profile settings for this specific task."
    },
    "gc_policy": {
      "type": "string",
      "enum": ["retain_all", "compact_on_complete", "compact_on_done", "archive_only"],
      "default": "compact_on_complete",
      "description": "Garbage collection policy for task context. Default is compact_on_complete for tasks."
    },
    "context_load_policy": {
      "type": "string",
      "enum": ["eager", "lazy", "on_demand", "minimal"],
      "default": "lazy",
      "description": "Context loading policy. Default is lazy for tasks."
    },
    "td_labels": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Additional labels for TD tracking."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Free-form tags for categorization."
    }
  },
  "additionalProperties": false
}
